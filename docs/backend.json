{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user profile within the NEU Access Hub, including authentication details and assigned roles.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "The institutional email address of the user, used for Google Sign-In and domain restriction.",
          "format": "email"
        },
        "studentId": {
          "type": "string",
          "description": "The student ID associated with the user's account, used for an alternative login method."
        },
        "role": {
          "type": "string",
          "description": "The assigned role of the user (e.g., 'user', 'admin'), determining access privileges."
        }
      },
      "required": [
        "id",
        "email",
        "studentId",
        "role"
      ]
    },
    "Visit": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Visit",
      "type": "object",
      "description": "Records a single visit entry by a user to the NEU Library.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Visit entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who made this visit. (Relationship: User 1:N Visit)"
        },
        "checkInTime": {
          "type": "string",
          "description": "Timestamp when the user checked into the library.",
          "format": "date-time"
        },
        "checkOutTime": {
          "type": "string",
          "description": "Optional timestamp when the user checked out of the library.",
          "format": "date-time"
        },
        "purpose": {
          "type": "string",
          "description": "The stated purpose or reason for the user's visit to the library."
        }
      },
      "required": [
        "id",
        "userId",
        "checkInTime",
        "purpose"
      ]
    },
    "Notification": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Notification",
      "type": "object",
      "description": "Represents an announcement or message displayed to authenticated users within the NEU Access Hub.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Notification entity."
        },
        "title": {
          "type": "string",
          "description": "The title or subject of the notification."
        },
        "message": {
          "type": "string",
          "description": "The main content or body of the notification message."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the notification was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "title",
        "message",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profiles, including email, student ID, and assigned role. The document ID (`userId`) must match `request.auth.uid`. This collection is central for role-based access control (DBAC)."
        }
      },
      {
        "path": "/visits/{visitId}",
        "definition": {
          "entityName": "Visit",
          "schema": {
            "$ref": "#/backend/entities/Visit"
          },
          "description": "A top-level collection storing institutional visit records. Authorized administrators can audit all entries, while users can access their own logs via the `userId` field."
        }
      },
      {
        "path": "/notifications",
        "definition": {
          "entityName": "Notification",
          "schema": {
            "$ref": "#/backend/entities/Notification"
          },
          "description": "A top-level collection for global announcements or messages. All authenticated users are allowed to read and list documents from this collection."
        }
      }
    ],
    "reasoning": "The Firestore structure has been synchronized with the application's flat data model for visits. This ensures that root-level queries used in the administrative overview and visitor logs are properly authorized without requiring hierarchical complexity. Security rules have been updated to permit administrative oversight while protecting user privacy."
  }
}